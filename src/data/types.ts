export type IconProps = {
  className: string;
};

export type HeroSlide = {
  id: number;
  img: string;
  heading: string;
  body: string;
};

export type ServiceCard = {
  id: number;
  heading: string;
  body: string;
  icon: JSX.Element;
};
